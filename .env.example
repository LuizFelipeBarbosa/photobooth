# Required: password users enter in the web login form.
PHOTOBOOTH_API_PASSWORD=change-this-password

# Recommended: random string to keep sessions valid across server restarts.
PHOTOBOOTH_SESSION_SECRET=replace-with-a-long-random-secret

# Set true only when serving behind HTTPS.
PHOTOBOOTH_SECURE_COOKIES=false
